# Generate and install policy from the denials
sudo ausearch -m avc -ts recent | audit2allow -M shield_nginx_gunicorn
sudo semodule -i shield_nginx_gunicorn.pp

# Restart nginx
sudo systemctl restart nginx

# Check for new denials
sudo ausearch -m avc -ts recent
